<div class="app">
  <div class="app__top">
      <div class="top-nav">
      <div class="top-nav__group">
        <div class="top-nav__logo">
        <img src="/images/bbbs.png" class="top-nav__logo" alt=""></div>
      </div>

      <div class="top-nav__group">
        {{#if session.isAuthenticated}}
        <a class="top-nav__item" href="#" {{action session.invalidate}}>Logout</a>
        {{else}}
          {{#link-to "index" class="top-nav__item" }} Login {{/link-to}}
        {{/if}}
        {{#link-to "calendar" class="top-nav__item"}} Calendar {{/link-to}}
        {{#link-to "index" class="top-nav__item" }} Messages {{/link-to}}
      </div>
      </div>
      <div class="hero">
      </div>
    </div>
  </div>



  <div class="app__content">
    {{ outlet }}
  </div>
