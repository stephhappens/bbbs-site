  <div class="container">
  <div class="grid">
    {{#each model as |user|}}
      <div class="card">
       {{#link-to "admin.index" class="grid__item"}} {{user.firstName}} {{user.lastName}} {{/link-to}}
      </div>
    {{/each}}
      </div>
      </div>
  {{!-- <div class="form__buttons">
    <button class="form__btn">Approve this User?</button>
  </div> --}}
